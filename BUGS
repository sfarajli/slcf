# [FIXED] trash command in lf doesn't work with files which has spaces in them.
- Emergency: low

# Using /tmp/noti.txt for notification is not secure
- Emergency: moderate

# Automatic slock build script causes problems with OOM
- Emergency: moderate

# Getopts invalid flag message uses full path instead of just the program name
- Emergency: low
